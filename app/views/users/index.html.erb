<h1>Users#index</h1>


<%= render 'books/index', newBook: @newBook %>

<h1>Users</h1>
<span>image</span><span>name</span>
<div>
<% @users.each do |users| %>
<%= image_tag users.get_profile_image(100,100) %>
<%= users.name %>
<%= link_to "Show", user_path(users.id) %>
<%end%>
</div>

